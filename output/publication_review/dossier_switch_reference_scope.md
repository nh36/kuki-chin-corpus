---
title: "Tedim same-subject and different-subject clause linkage scoping dossier"
---

# Scope and status

This is the first candidate-first pass for the switch-reference / same-subject and different-subject clause-linkage question. The safest print-facing claim is narrower than the report's headline: same-subject converb linkage is secure, while the different-subject-looking material remains support-only.

The scope stays narrow. It does **not** attempt a full subordination chapter, a full relative-clause chapter, a full converb chapter, or a full discourse chapter.

## Example display stabilization

The current display decision is now more conservative and cleaner than the first pass.

- `bawlin` remains promoted, but the Genesis 11:4 quotation is trimmed so the target `bawl-in` is not buried under `amaute in`, `nadingin`, `a dawn in`, or `lamin`.
- `semin` remains promoted; Deuteronomy 10:20 is kept in full because the same-subject chain is already compact, and `beel-in` stays boundary material rather than a second converb-like anchor.
- `ahih ciangin` remains support-only and table-only.
- `ngenin` remains blocked, and ordinary `ciangin`, `dingin`, `a bawl mi`, `omna`, and `muhna-ah` remain boundary rows.

# Evidence base

Primary sources for this scope:

- `docs/grammar/reports/08-clause-02-switch-reference.md`
- `docs/grammar/reports/08-clause-01-subordination.md`
- `docs/grammar/reports/08-clause-03-relatives.md`
- `docs/grammar/lit-reviews/08-clause-03-subordination-lit.md`

Current controlling layer:

- `output/publication_review/candidates_switch_reference.tsv`

# Candidate sorting

## Promoted same-subject candidates

- `sr-bawlin-gen11p4` (`bawlin`, Genesis 11:4)
- `sr-semin-deut10p20` (`semin`, Deuteronomy 10:20)

These rows are the secure core. They are source-resolved, same-subject, and morphologically clear enough to lead the narrow first slice.
No equally clean Gospel example is currently used, so the core stays OT-only for now.

## Support-only different-subject-looking candidate

- `sr-ahih-ciangin-gen1p21` (`ahih ciangin`, Genesis 1:21)

This row keeps the report's different-subject-looking pattern visible, but it does not justify a full switch-reference claim. It remains a table row rather than a formal example.

## Blocked report anchor

- `sr-ngenin-gen41p55` (`ngenin`, Genesis 41:55)

This row stays blocked because analyzer output does not keep the morphology stable enough for promotion.

The printed `ngenin` material should continue to be treated as blocked/unresolved, not as a secure same-subject diagnostic.

## Boundary rows

- `sr-ciangin-gen1p3`
- `sr-dingin-gen1p14`
- `sr-abawlmi-ex30p38`
- `sr-omna-gen4p16`
- `sr-muhnaah-gen6p11`

These rows remain boundary-controlled. They keep temporal subordination, purposive boundary material, relative clauses, and nominalization visible without widening the packet.

# Safest first grammar-facing label

The report's `switch reference` term is too strong for the print-facing slice. The safer label is **same-subject and different-subject clause linkage**.

That label keeps the secure same-subject material visible, keeps the weaker different-subject-looking evidence explicit, and avoids implying that Tedim has a fully settled switch-reference system at this stage.
