---
title: "Review Notes: Tedim TAM / Aspect / Modal Print Slice"
---

# What works

The TAM / aspect / modal packet is now aligned at the current slice maturity level. It has a candidate TSV, a scoping dossier, a grammar print slice, a dictionary print slice, and tests covering the compact first-slice contrasts: `candidates_tam.tsv`, `dossier_tam_scope.md`, `grammar_tam_print_slice.md`, `dictionary_tam_print_slice.md`, and the associated test files. The grammar and dictionary slices are controlled by `candidates_tam.tsv`, interpreted through the scoping dossier `dossier_tam_scope.md`, and cross-checked against `grammar_tam_print_slice.md` plus `dictionary_tam_print_slice.md`, not by raw generated-report counts and not by broad harvesting of every TAM-looking suffix or stack.

The core first-slice analysis is now synchronized in the right way. `Paingei` is the current experiential `-ngei` anchor. `Neigige` is the current habitual `-gige` anchor. `Paizel` is usable as habitual-continuative `-zel` evidence, but only with its construction-control caveat. `Kilawmta` is usable as perfective or completed-event `-ta` evidence, but only with its sentence-final overlap caveat. `Bawlzo` is usable as completive `-zo` evidence, but only with its bare-`zo` and sentence-final overlap caveat. `Hongpaikik` is usable as iterative or 'again' `-kik` evidence, but only with its motion/return caveat. `Omding` is the compact irrealis / future / modal `-ding` anchor, but only with its `dingin` and clause-bound caveat. `Bawlthei` is the compact abilitative `-thei` anchor, but only with its negation/irrealis-stack caveat.

This packet is intentionally narrow. It is a first TAM packet, not a full TAM chapter.

# What does not yet work

The packet does not yet describe the full TAM system, and it does not yet analyze all VP-slot stacking. It does not yet resolve all sentence-final overlap around `ta` and `zo`. It does not promote `-lai` / `pailai`, and it does not promote `dingin` or other clause-bound `-ding` forms as the main dictionary or grammar anchor.

It also does not use negative modal stacks such as `khiathei ding om lo` as core evidence. It does not use directional/TAM stacks such as `khia-ta` as core evidence. It does not promote `bawlzoding` or `bawlsakthei` as first-slice dictionary or grammar anchors. It does not promote `-nawn` or `-khin` until cleaner anchors are selected.

# Analyzer/export and overlap caveats

The main caveats are now explicit and manageable. `Paizel` is usable, but it still borders broader continuative aspect and must stay construction-controlled. `Kilawmta` is usable as compact `-ta` evidence, but only with the sentence-final overlap caveat kept visible. `Bawlzo` is usable as compact `-zo` evidence, but only with the bare-`zo` and sentence-final overlap caveat kept explicit. `Hongpaikik` is usable as iterative or 'again' evidence, but it should not be widened into a motion/return chapter.

`Omding` is the right compact `-ding` anchor for now, but `dingin` and other clause-bound `-ding` material remain overlap controls rather than the main print anchor. `Bawlthei` is the right compact `-thei` anchor for now, but negative-modal or irrealis-stacked strings such as `khiathei ding om lo` remain overlap controls rather than core first-slice evidence.

These caveats are reasons to keep the candidate TSV, dossier, grammar slice, dictionary slice, and review notes aligned. They are not reasons to reopen the packet into a broad TAM rewrite.

# Print-slice cautions

The following claims are safe at the current slice maturity level:

- `-ngei` as experiential evidence through `paingei`;
- `-gige` as habitual evidence through `neigige`;
- `-zel` as habitual-continuative evidence through `paizel`, only with construction-control caveat;
- `-ta` as perfective / completed-event evidence through `kilawmta`, only with sentence-final overlap caveat;
- `-zo` as completive evidence through `bawlzo`, only with bare-`zo` and sentence-final overlap caveat;
- `-kik` as iterative / again evidence through `hongpaikik`, only with motion/return caveat;
- `-ding` as irrealis / future / modal evidence through `omding`, only with `dingin` and clause-bound caveat;
- `-thei` as abilitative evidence through `bawlthei`, only with negation/irrealis-stack caveat.

The following should stay out of print for now:

- full-system TAM claims;
- all-purpose VP-slot stacking claims;
- unresolved sentence-final overlap around `mangngilh ta hi` or bare `zo`;
- `pailai` as a promoted first-slice anchor;
- `dingin` or other clause-bound `-ding` forms as the main print anchor;
- negative modal stacks such as `khiathei ding om lo` as core evidence;
- directional/TAM stacks such as `khia-ta` as core evidence;
- broader VP-slot stacks such as `bawlzoding` and `bawlsakthei`;
- report-summary items such as `-nawn` and `-khin` before cleaner anchors are selected.

# Recommended next editorial task

With these review notes added, the TAM packet is now ready for human review at the current slice maturity level. The next substantive work should be either a reviewer-identified correction to an existing packet or explicit selection of a new narrow scope, not another open-ended TAM expansion.
